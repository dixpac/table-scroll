<%= turbo_frame_tag(
      "next-page-#{@pagy.next}",
      # autoscroll: true,
      loading: :lazy,
      src: pagy_url_for(@pagy, @pagy.next, format: :turbo_stream),
      target: "_top"
    ) do %>
  Loading...
<% end %>
